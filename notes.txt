#### event_url_scraper.py ####

The website https://ifsc.results.info/ uses Vue.js and has a volatile structure, this makes it hard to use selenium sustainably

Playwright is a newer package that is better at handling the backend APIs of websites.

The API structure is https://ifsc.results.info/api/v1/ for the home page and then https://ifsc.results.info/api/v1/seasons/3 is the 
year 1990, up to https://ifsc.results.info/api/v1/seasons/39 for the year 2028. There is also an https://ifsc.results.info/api/v1/info

For an unknown reason 2020 is season = 2 and there is yet to be a 2027 assigned.

Currently you cannot get the API directly and need to replicate browser access. Playwright can do this.

Each eventID has it's own url (e.g. https://ifsc.results.info/api/v1/events/1492 ). Within each event are the seperate disciplines
(e.g. https://ifsc.results.info/api/v1/events/1492/result/313).

A raw json is now extracted so that you can see the structure and decide which elements to extract.

TO DO:
* Amend the script to only incrementally add new rows to an existing events_data list AND only look at the new urls in that list.
* Check text formatting so that special characters export to csv correctly.

#### extract_JSON_from_urls.py ####

Added complication that events with Q-SF-F structure will have additional event pages with a url 
such as https://ifsc.results.info/api/v1/category_rounds/8584/results

Use a JSON tree viewer online to understand the JSON structure.

I will need to decide which elements to pull into the flat csv version, or can I just use the JSONL for 
further steps?

TO DO:
* Can it be scraped faster without rate-limit
* Understand required output
* Adjust flat csv output
* Add back in ability to query a single JSON from a specific URL / single year - have a seperate script for this?

#### Emma conversation 28/04 ####
* Send an example output ASAP
* Replicate the previous output CSV structure. but also provide a flat csv of all columns

#### extract_results_JSON_layers.py ####

On first run this will try and loop through ~21k pages and take 5.5 hours

TO DO:
* Create a duplicate script to pull only one event / year